
doctype html

block variables
	- let base = "."
	- let pageSubtitle

head
	meta(charset="utf-8")
	meta(name="viewport" content="width=device-width,initial-scale=1")
	title xiome.io#{pageSubtitle && " – " + pageSubtitle}

	script(defer type="importmap-shim" src=`${base}/../importmap.json`)
	script(async defer type="module-shim" src=`${base}/../xiome.js`)
	script(async defer src="https://unpkg.com/es-module-shims@0.9.0/dist/es-module-shims.js")

	<xiome-config app=3e7a713d4412644e5afa6a598337c8f2e53bdfefc8cb6626c31e69aa5df9a7cd api="http://localhost:4999"></xiome-config>

	style.
		html, body {
			background: #333;
			color: #999;
		}

h1 mock site
xiome-login-panel(show-logout)
	xiome-my-account
		h3 your account
