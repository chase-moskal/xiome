
/*

// concept: a lit-html replacement

function render() {
	return bulldog`

		// supports comments
		<h1> indents instead of closing tags

		<p>
			this is content,
			and it wraps

		<ul>
			<li> item 1
			<li> item 2

		<div attributes=easy>

		<xio-text-input
			?booleans=${truthy()}
			.properties=${justLikeLit()}
			@click=${eventHandlersToo}
			...${spreadAttributes}
			....${spreadProps}
			?...${spreadBools}
			@...${spreadHandlers}>
				inner content

	`
}

*/
